import React from "react";

function Continue() {
   return (
      <div className="flex justify-center ">
         <button className="text-white font-bold text-lg bg-slate-800 p-3 mt-5 rounded-full h-max w-full hover:bg-slate-700">
            Continue
         </button>
      </div>
   );
}

export default Continue;
